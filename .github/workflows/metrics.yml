# File: metrics.yml
# Description: Hourly lowlighter/metrics run that regenerates github-metrics.svg
#   (profile header, activity, languages, isocalendar, lines) for README.md.
# Author: Jose-Jorge HERNANDEZ
# Company: Parlee Conseiller, Inc.
# Date: 2026-06-19
# Last edit date: 2026-09-24
# Version: 1.1.0

# Visit https://github.com/lowlighter/metrics#-documentation for full reference
name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{cron: "0 * * * *"}]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          # The following scopes are required:
          #  - public_access (default scope)
          # The following additional scopes may be required:
          #  - read:org      (for organization related metrics)
          #  - read:user     (for user related data)
          #  - read:packages (for some packages related data)
          #  - repo          (optional, if you want to include private repositories)
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          user: josejorge
          template: classic
          base: header, activity, community, repositories, metadata
          base_hireable: yes
          config_display: large
          config_octicon: yes
          config_timezone: America/Mexico_City
          config_twemoji: yes
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repositories: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          # RTF is document files, not code; keep it out of the languages chart
          plugin_languages_ignored: rich text format
          plugin_languages_limit: 12
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
          plugin_lines: yes
          plugin_lines_history_limit: 1
          plugin_lines_repositories_limit: 4
          # GitHub reports a phantom contributor with a null author for this repo, which crashes the
          # lines plugin (TypeError reading "login") and fails the whole run; skip the repo until fixed upstream
          plugin_lines_skipped: personal-automations
          plugin_lines_sections: base
